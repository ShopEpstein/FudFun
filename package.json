{
  "name": "fudfun",
  "private": true,
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "postinstall": "npm install --prefix server",
    "build": "echo 'No build step required'",
    "start": "node src/index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
